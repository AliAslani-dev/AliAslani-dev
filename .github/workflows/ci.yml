
name: CI/CD Pipeline

on:
  push:
    branches:
      - test

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: عملیات deploy
        run: echo "اجرای عملیات deploy روی برنچ test"
